package com.github.eterdelta.broomsmod;

import com.github.eterdelta.broomsmod.entity.WoodenBroomEntity;
import net.minecraft.network.chat.Component;
import net.minecraft.world.entity.Entity;
import net.minecraft.world.entity.player.Player;
import net.neoforged.bus.api.SubscribeEvent;
import net.neoforged.neoforge.event.ServerChatEvent;

public class TestingChatEvent {
/*
    @SubscribeEvent
    public void chatChat(ServerChatEvent event)
    {
        Player player = event.getPlayer();
        Entity vehicle = player.getVehicle();
        if (vehicle instanceof WoodenBroomEntity broom)
        {
            int a = broom.getSeaBreezeLevel();
            player.displayClientMessage(Component.literal(String.valueOf(a)), true);
        }

    }
*/
}
